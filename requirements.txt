# Pinned to the versions verified present in the dev environment (Python 3.13.1).
# Runtime
pandas==2.3.3
numpy==2.3.3
pyarrow==22.0.0
polars==1.35.2
numba==0.62.1
plotly==6.3.1
tqdm==4.67.1
joblib==1.5.2
scipy==1.16.2
requests==2.32.5
python-dotenv>=1.0
pydantic>=2.7
pyyaml>=6.0

# Optional (parameter search) — install with: pip install -r requirements.txt optuna
# optuna>=3.6

# Dev
# pytest>=8.0
# ruff>=0.5
